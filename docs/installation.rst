.. _installation:

Installation
============

* To install agon_ratings::

    pip install marturion

* Add ``marturion`` to your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # other apps
        "marturion",
    )
